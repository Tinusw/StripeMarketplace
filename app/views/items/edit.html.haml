.col-xs-12.col-sm-12.col-md-12.col-lg-12
  .jumbotron.text-center
    %h4
      = @item.title
    = render 'form', item: @item, merchant: @merchant
    .container
      = link_to 'Back', merchants_path(@merchant), class: 'btn btn-info'
